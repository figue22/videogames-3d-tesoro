using UnityEngine;

public class AnimationFixer : MonoBehaviour
{
    // Estas funciones simplemente "atrapan" el evento para que no haya error
    void OnFootstep(AnimationEvent animationEvent) { }
    void OnLand(AnimationEvent animationEvent) { }
}